syntax = "proto3";

package doccreator;

service TTSService {
  rpc Synthesize (TTSRequest) returns (TTSResponse);
}

message TTSRequest {
  string text = 1;
  string voice = 2;
}

message TTSResponse {
  bytes audio = 1;
}
